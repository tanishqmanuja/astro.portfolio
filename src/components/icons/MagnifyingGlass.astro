---
//! radix-icons | MIT License | https://icons.modulz.app/
//! Vendored locally to replace unmaintained `@astropub/icons`
//! (its `export { Props } from "./Props.ts"` breaks Vite 8 / Rolldown builds).
import type { Props } from "./props";

let { size, title, width = size, height = size, ...props }: Props = {
  fill: "CurrentColor",
  "fill-rule": "evenodd",
  title: "Magnifying Glass",
  viewBox: "0 0 15 15",
  ...Astro.props,
};

const toAttributeSize = (size: number | string | undefined) =>
  size == null ? size : String(size).replace(/(?<=[0-9])x$/, "em");

size = toAttributeSize(size);
width = toAttributeSize(width);
height = toAttributeSize(height);
---

<svg {width} {height} {...(props as Record<string, any>)}
  >{title ? <title>{title}</title> : ""}<path
    d="M10 6.5a3.5 3.5 0 1 1-7 0 3.5 3.5 0 0 1 7 0Zm-.691 3.516a4.5 4.5 0 1 1 .707-.707l2.838 2.837a.5.5 0 0 1-.708.708L9.31 10.016Z"
  /></svg
>

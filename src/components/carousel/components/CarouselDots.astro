---

---

<div class="embla__dots"></div>

<style is:global>
  .embla__dots {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    align-items: center;
    margin-right: calc((2.6rem - 1.4rem) / 2 * -1);
  }
  .embla__dot {
    -webkit-tap-highlight-color: rgba(var(--color-text-secondary), 0.5);
    -webkit-appearance: none;
    appearance: none;
    background-color: transparent;
    touch-action: manipulation;
    display: inline-flex;
    text-decoration: none;
    cursor: pointer;
    border: 0;
    padding: 0;
    margin: 0;
    width: 2.6rem;
    height: 2.6rem;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }
  .embla__dot:before,
  .embla__dot:after {
    width: 16px;
    border-radius: 4px;
    aspect-ratio: 1;
    position: absolute;
    display: flex;
    align-items: center;
    content: "";
    transition: all 0.2s ease-in-out;
  }
  .embla__dot:before {
    border: 0.2rem solid var(--color-text-subdued);
  }
  .embla__dot:after {
    border: 0.2rem solid var(--color-text-secondary);
    opacity: 0;
  }
  .embla__dot--selected:before {
    border-radius: 8px;
  }
  .embla__dot--selected:after {
    border-radius: 8px;
    background-color: var(--color-barely-visible);
    opacity: 1;
  }
</style>
